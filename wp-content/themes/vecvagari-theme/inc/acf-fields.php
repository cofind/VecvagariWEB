<?php
/**
 * LRM-130: ACF field registration — superseded.
 *
 * The ACF PHP-local approach was replaced with a custom WP Admin settings page
 * (inc/admin-homepage.php) because PHP-local field groups conflict with ACF's
 * Gutenberg REST API integration and never rendered in the page editor.
 *
 * ACF plugin may remain installed but is no longer used for homepage fields.
 *
 * @package VecvagariTheme
 */
